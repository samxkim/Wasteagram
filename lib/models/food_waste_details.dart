class WasteDetail {
  final DateTime date;
  String imageURL;
  double latitude;
  double longitude;
  int quantity;

  WasteDetail({this.date, this.imageURL, this.latitude, this.longitude, this.quantity});
}